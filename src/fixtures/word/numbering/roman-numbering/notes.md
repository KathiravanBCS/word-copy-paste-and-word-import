# numbering/roman-numbering

`mso-level-number-format:roman-upper` with level text `"%1\."`.

This is the shape that maps cleanly onto a CSS counter style, so the renderer
produces a genuine `list-style-type` and the browser draws `I.`, `II.`, `III.`
as native markers.
