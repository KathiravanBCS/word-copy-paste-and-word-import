# images/inline-image

A picture whose bytes are actually present, as a `data:` URI.

This is the good case: `resolution: "resolved"`, `origin: "data-uri"`, and a
real `<img>` in the output with the dimensions Word stated.
