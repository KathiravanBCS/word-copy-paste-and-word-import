# mixed/list-continuation

The same list (`l0`, `lfo1`) resuming after an interrupting paragraph. Word
continues the numbering at 3; the model must record both runs as the same list
instance, and the rendered output must not restart at 1.
